.BottomBar {
  width: 100%;
  position: fixed;
  bottom: 0;
  left:0;
  right:0;
  z-index: $indexLevel.three;

  &__content {
    background: $palette.white;
    box-shadow: 0 0 5px $palette.darkGrey;
    padding: 10px 40px;
  }
}
